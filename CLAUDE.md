# Project Rules

- User has Playwright CLI installed. Always use Playwright to test frontend changes yourself instead of asking the user to verify.
- 排查问题时禁止全项目代码扫描。先读 memory/project_structure.md 获取项目全貌，再定位到具体模块，只探索相关文件。用 haiku 模型做 Explore 子任务以节省 token。
